# from random import choice 
# from numpy import array, dot, random

# training_data = [ 
#   (array([0,0,1]), 0), 
#   (array([0,1,1]), 1), 
#   (array([1,0,1]), 1), 
#   (array([1,1,1]), 1), 
# ]
# print training_data


unit_step = lambda x: 0 if x<0 else 1